var askedPhrase = prompt("Write down any phrase, please");

var length = 0;
while (!askedPhrase.equals("")) {
    askedPhrase = askedPhrase.substring(0);
    ++length;
}
alert(length);

function phraseSize(phrase) {
  for(var howManyChar = 0, howManyChar <= size, howManyChar++)
  return howManyChar;
  }
}
alert(phraseSize(askedPhrase));




console.log(askedPhrase.toUpperCase());
